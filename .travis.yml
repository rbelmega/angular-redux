sudo: required
language: node_js
node_js:
  - "10.15.1"
services:
  - docker
dist: trusty
branches:
  only:
  - master
before_script:
  - npm install -g @angular/cli

script:
- ng lint
- ng build
- docker build -t rbelmeha/angular-redux .
